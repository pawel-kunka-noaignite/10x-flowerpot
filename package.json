{
  "name": "flowerpot",
  "version": "0.0.0",
  "private": true,
  "description": "Flowerpot — houseplant care scheduling (web/PWA).",
  "workspaces": [
    "shared",
    "frontend",
    "api"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @flowerpot/frontend",
    "build": "npm run build --workspace @flowerpot/frontend && npm run build --workspace @flowerpot/api",
    "test": "npm run test --workspace @flowerpot/frontend"
  }
}
