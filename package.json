{
  "name": "10x-flowerpot",
  "version": "0.0.0",
  "private": true,
  "description": "Flowerpot — houseplant care scheduling (web/PWA).",
  "workspaces": [
    "shared",
    "frontend",
    "api"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @10x-flowerpot/frontend",
    "build": "npm run build --workspace @10x-flowerpot/frontend && npm run build --workspace @10x-flowerpot/api",
    "test": "npm run test --workspace @10x-flowerpot/frontend"
  }
}
