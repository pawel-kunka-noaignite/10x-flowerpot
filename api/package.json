{
  "name": "@10x-flowerpot/api",
  "version": "0.0.0",
  "description": "Flowerpot Azure Functions API",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "vitest run"
  },
  "dependencies": {
    "@10x-flowerpot/shared": "*",
    "@azure/data-tables": "^13.0.0",
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4"
  }
}
